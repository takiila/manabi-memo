# 時間割・講義JSON

設定 →「時間割・講義情報のJSON」から利用します。

1. フォームでJSONを作る、見本を編集する、または「AIへの依頼文」をコピーしてAIに作成を依頼します。
2. JSONを貼り付けるかファイルを選び、「取り込み内容を確認」を押します。
3. 新規講義は追加されます。既存講義は更新チェックを付けたものだけ変更します。空欄も更新対象です。
4. 「確認した内容を適用」を押し、画面上部で保存完了を確認します。
5. 講義を開くと評価方法・自由メモ・単位数・シラバスURLを確認できます。

同一の学期 `term` と科目コード `code` は同じ講義です。従来のコード未設定講義は同学期・同名で照合します。講義IDを維持して更新するのでノート・PDF・付箋は残ります。ごみ箱・保管中の一致は復元してから取り込んでください。同じ時間枠の講義は警告されるため、適用前に調整してください。

形式は `format: "manabi-course-pack"`, `version: 1`, `courses: [...]` です。各講義の必須項目は `code`, `term`, `title`。任意項目は `instructor`, `room`, `weekday`, `period`, `credits`, `assessment`, `notes`, `syllabusUrl` です。見本と詳細仕様は画面からコピーできます。未対応の項目・版は黙って捨てずエラーにします。

`weekday` は月=1〜土=6、`period` は90分枠の1〜5。大学の1〜2限は1、3〜4限は2、5〜6限は3です。時刻は現行時間割に合わせて08:40、10:20、12:40、14:20、16:00開始です。自由な時刻・日曜・複数枠の一括指定には未対応です。

「講義JSONを書き出す」は保管・削除中を除く全学期の講義を日時付きファイルに保存します。コード未設定講義には安定した `local-講義ID` を出力します。これは講義情報専用であり、ノート・PDF・課題の進捗を移す完全バックアップとは別です。更新前の講義JSONを保管すると戻す際にも使えます。

個人JSONは非公開の個人保管先で管理し、公開コードやサンプルに混ぜないでください。複数PCには同じJSONを手動で取り込みます。JSONの更新だけでは別端末は自動更新されません。
