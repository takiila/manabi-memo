# Codexと進めた開発

この文書は、生成AIを利用した事実を隠さず、何を人間が決め、Codexに何を任せ、どのように結果を検証したかを説明するためのものです。生の会話ログや秘密情報は公開しません。

## 役割分担

### 人間が担当したこと

- 大学生としての利用場面と、解決したい不便の定義
- 時間割→講義→授業回→ノート／PDF／付箋という主要導線
- 個人ノート、GPA、卒業要件を他人へ共有しない境界
- 2〜3人で共有する課題、確認事項、遊び候補と本人別回答の仕様
- 「ログインしただけで端末データを送らない」などの安全要件
- 優先順位、採否判断、物理スマートフォンでの操作とfeedback
- 最終的に完成・未完成を判定する受入条件

### Codexに任せたこと

- 旧ChatGPT Site版と現行Next.js版のsource inventory
- Next.js、IndexedDB、SQLite／PostgreSQL、オブジェクトストレージの実装
- 保存形式、同期競合、PDF整合性、共有カレンダーの回帰テスト
- 不具合のsource追跡、修正候補の実装、型・lint・build検証
- README、構成資料、デプロイ手順の更新

### 共同で行ったこと

- 要望をobservableな完了条件へ変換する
- 実装結果を一次コードとテストで確認する
- 実機feedbackをTODO、設計ルール、回帰テストへ戻す
- 未確認事項を、実装済み／対象環境でverifiedの二段階に分ける

## 非公開の開発記録

開発中の判断と検証状態は、公開リポジトリには含めない別のprivate Markdown repositoryで管理しています。会話を丸ごと保存するのではなく、次の役割へ整理します。

| 文書 | 役割 |
|---|---|
| PROJECT | 長期目的、対象利用者、壊してはいけない制約 |
| CURRENT | 現在の検証済み状態と参照commit |
| TODO | 未完了作業とobservableな完了条件 |
| DECISIONS | なぜその設計を選んだか |
| FEATURES | 旧版からの移行・機能差分 |
| JOURNEYS | 利用者が目的を達成するstart-to-resultの主導線 |
| FEEDBACK | 実利用で起きた成功・摩擦・失敗 |
| PROVENANCE | idea→decision→implementation→acceptanceの追跡 |

非公開記録は実装証拠の代わりにはしません。今回直接読んだsource、Git、test、build、実ブラウザ、物理端末の結果を優先し、矛盾した記録は更新します。API key、token、Cookie、実メール、個人noteやPDFは保存しません。

## 実機feedbackから改善した例

物理スマートフォンで「端末のノートを読み込んでいます」から進まない問題が見つかりました。最初の修正はIndexedDB接続だけを8秒で区切りましたが、source再監査で、その後のstate取得要求が未解決のまま待てることと、期限後に遅れて開いたDBを閉じられないことを確認しました。

そこで、接続からstate取得までを1つのdeadlineで囲み、期限時のtransaction中断、DB close、遅延接続のclose、既存データを上書きしないerror／retry導線を追加しました。正常、読取停止、遅延接続の回帰テストを追加し、71件のtest、build、390×844の通常起動まで再確認しています。同じ問題端末での再受入は未完了のため、verifiedとは表現していません。

## Codexへ与えるguardrail

[AGENTS.md](AGENTS.md) に、削除してはいけない機能、保存境界、同期の安全条件、変更時に必要なテストを記録しています。特に次を固定しています。

- 端末保存を先行し、同期は利用者の明示操作後だけ開始する
- 別アカウントの端末データを確認なしに送らない
- PDFのrevision、noteVersion、SHA-256検証を外さない
- 個人データと共有カレンダーを同じ保存領域へ入れない
- 読込失敗を理由にIndexedDB、note、PDFを自動削除しない
- UI変更はPC幅とスマートフォン幅の両方で確認する

## 現在の限界

- 現行の中心画面 `app/page.tsx` は旧版互換を優先した大きなorchestratorで、段階的な分割が必要です。
- 実Firebase、Neon、Cloudflare R2を接続した公開環境の受入は未完了です。
- 問題が出た物理スマートフォンで、起動からnote／PDF保存までの再受入が必要です。
- 旧公開版との全機能を1対1で比較する監査は未完了です。

このプロジェクトで示したいのは、生成AIへ一度にアプリを作らせたことではなく、利用者の課題を仕様と安全条件へ変換し、Codexの出力を検証し、実利用の失敗を次の設計とtestへ戻す開発経験です。
