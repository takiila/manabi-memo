# Windowsでの起動方法

## 最も簡単な方法

1. ZIPファイルを右クリックし、「すべて展開」を選びます。
2. 展開した `manabi-memo-local` フォルダを開きます。
3. `START_WINDOWS.cmd` をダブルクリックします。
4. 初回だけ必要なパッケージが自動で導入されます。
5. 画面に起動完了が表示されたら、ブラウザで `http://localhost:3000` を開きます。

Windowsから警告が表示された場合は、ファイル名が `START_WINDOWS.cmd` であり、展開したプロジェクト内のファイルであることを確認してから実行してください。

## Node.jsが入っていない場合

Node.js 22.13以上を先にインストールしてください。

- 公式サイト: <https://nodejs.org/>
- インストール時は標準設定のままで構いません。
- インストール後、開いているコマンド画面を閉じてから `START_WINDOWS.cmd` をもう一度実行します。

## 手動で起動する場合

プロジェクトフォルダでPowerShellを開き、次を順に実行します。

```powershell
npm install
npm run dev
```

その後、ブラウザで `http://localhost:3000` を開きます。

## 途中で止まった場合

エラー画面を閉じず、赤字を含めて最後の20行ほどが見えるスクリーンショットを残してください。原因の判定には、最初のエラー行と末尾のエラーコードが重要です。
